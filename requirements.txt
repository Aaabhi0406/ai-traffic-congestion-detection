# Pinned to known-compatible minor versions. These are reasonable defaults —
# for an exact reproducible environment, activate your venv and run
# `pip freeze > requirements.txt` to replace this with your actual versions.
ultralytics==8.2.100
deep-sort-realtime==1.3.2
opencv-python==4.10.0.84
streamlit==1.38.0
scikit-learn==1.5.2
pandas==2.2.3
numpy==1.26.4
